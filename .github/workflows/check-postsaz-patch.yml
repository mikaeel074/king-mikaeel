name: Check Postsaz 5.2 patch

on:
  push:
    paths:
      - postsaz_5.2_patch.py
      - .github/workflows/check-postsaz-patch.yml
  workflow_dispatch:

jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m py_compile postsaz_5.2_patch.py
